using UnityEngine;

public class CollisionData : MonoBehaviour
{
    //Declare Variables
    public bool objectIsEnviromental; //If attached object is a part of the enviroment
    public bool objectIsPlayer; //If attached object is a player.


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
